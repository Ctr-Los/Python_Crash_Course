
#If statements

#If statements allow you to see the current state of the program and see the conditions.
#This chapter will teach you how to write conditional statements.

cars = ['audi','bmw','subaru','toyota']

for car in cars:
  if car == 'bmw':
    print(car.upper())
  else:
    print(car.title())

#Here python checks to see if the current value of car is bmw
#and if it IS then it will print the value in uppercase
#BUT if the value is anything other than bmw then the other values will be printed that are not bmw are printed in title.

#Conditional tests

#At the heart of every if statement, it can be seen as a true/false approach. In the case of python it is called a conditional test.
#meaning it will determine if the conditions are met and should be executed.
#When the condition is true, the code will execute, and if it is false then the code will be ignored.

#Checking for equity

#Most conditional test are confirmed when the current value of a variable to a specific value of interest.

car = 'bmw'
car == 'bmw' #car is assigned to bmw. Is car equal to bmw?

#Here is an example when it is false

car = 'audi'
car == 'bmw'

#Case sensitive

#Testing for equality is important especially when it comes to case sensitive

car = 'Audi'  #here I made audi uppercase, this will turn out to be false.
car == 'audi'

#Now if you just dont care about the case, and just want to test the value of a variable

car = 'Audi'
car.lower() == 'audi'

#Here is an example of the code being case-insesitive, meaning that upper-case and lower-case values are the same.

#Checking for inequality

#When you want to see if two values are not equal, you can use the != sign this means "not"


#Here's an example:

requested_topping = 'mushrooms'

if requested_topping != 'anchovies':
  print("Hold the anchovies")

  #The value for requested_toppings is not anchovies, the print statement is executed.

#Numerical Comparisons

#Pretty much the same thing when it came to strings

age = 18

age == 18

#You can also see if the two numbers are not the same.


answer = 17

if answer != 42:
  print('That is not the correct answer. Please try again!') #Here the test passes because 17 is not equal to 42 and therefore the
  #code block is executed.

#You can add other mathematical expressions such as < or > and <= >=

age = 19
age < 21

age <= 21

age > 21

age >= 21

#Checking multiple conditions

#Python allows you to check multiple conditions, using the keyword "and" you can check if both conditions are true.
#It's simple, if both are true then its true if both are false then its false. And if only one of them is false and the other is true it is still false.

age_0 = 22
age_1 = 18
age_0 >= 21 and age_1 >= 21

#Here age_0 is true but age_1 is false because 18 is not greater than or equal to 21. Therefore this is FALSE
